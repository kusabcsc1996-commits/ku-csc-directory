# 📁 คู่มือการเชื่อมต่อ Google Drive API สำหรับ KU CSC Hub

คู่มือนี้จะแนะนำวิธีเชื่อมต่อเว็บไซต์ **KU CSC Hub** เข้ากับ **Google Drive** ของท่าน เพื่อให้อัปโหลดรูปภาพแบนเนอร์ โลโก้ และภาพพื้นหลังเข้าไปเก็บไว้ใน Google Drive โดยตรง และได้ลิงก์ถาวรแสดงบนเว็บได้ฟรี ตลอดไป

---

## 🚀 ขั้นตอนการติดตั้ง (ทำเพียงครั้งเดียว ประมาณ 2 นาที)

### ขั้นตอนที่ 1: สร้าง Google Apps Script
1. เปิดเว็บเบราว์เซอร์ไปที่: **[script.google.com](https://script.google.com/)**
2. คลิกปุ่ม **"+ โครงการใหม่ (+ New project)"** ที่มุมซ้ายบน
3. ตั้งชื่อโครงการที่มุมบนซ้าย เช่น: `KU-CSC-Drive-API`
4. ลบโค้ดเริ่มต้นทั้งหมดในหน้าต่าง แล้วเปิดไฟล์ [google-apps-script-code.gs](google-apps-script-code.gs) ในโฟลเดอร์นี้ คัดลอกโค้ดทั้งหมดไปวางแทน

---

### ขั้นตอนที่ 2: ทำให้ใช้งานได้ในรูปแบบ Web App (Deploy)
1. คลิกปุ่มสีน้ำเงิน **"การทำให้ใช้งานได้ (Deploy)"** ที่มุมขวาบน > เลือก **"การทำให้ใช้งานได้ใหม่ (New deployment)"**
2. ที่รูปฟันเฟืองด้านซ้าย เลือกประเภทเป็น **"เว็บแอป (Web app)"**
3. ตั้งค่า 3 จุดดังนี้:
   - **คำอธิบาย (Description):** `KU CSC Drive API v1`
   - **ดำเนินการในฐานะ (Execute as):** เลือก **`ฉัน (Me - อีเมลของคุณ)`**
   - **ผู้ที่มีสิทธิ์เข้าถึง (Who has access):** เลือก **`ทุกคน (Anyone)`** *(สำคัญมาก เพื่อให้หน้าเว็บส่งไฟล์เข้ามาได้)*
4. คลิกปุ่ม **"การทำให้ใช้งานได้ (Deploy)"**
5. หากมีหน้าต่างขอสิทธิ์เข้าถึง ให้คลิก:
   - **"ให้สิทธิ์การเข้าถึง (Authorize access)"**
   - เลือกบัญชี Google ของท่าน
   - หากขึ้นเตือน *"Google ยังไม่ได้ตรวจสอบแอปนี้"* ให้คลิก **"ขั้นสูง (Advanced)"** > คลิก **"ไปที่ KU-CSC-Drive-API (ไม่ปลอดภัย)"**
   - คลิก **"อนุญาต (Allow)"**

---

### ขั้นตอนที่ 3: คัดลอก URL เว็บแอป
1. เมื่อ Deploy เสร็จแล้ว จะมีหน้าต่างแสดง **"URL เว็บแอป (Web app URL)"** ขึ้นมา หน้าตาจะเป็นแบบนี้:
   `https://script.google.com/macros/s/AKfycbx.../exec`
2. คลิกปุ่ม **"คัดลอก (Copy)"**

---

### ขั้นตอนที่ 4: นำ URL ไปใส่ในหน้าเว็บ KU CSC Editor
1. เปิดหน้าเว็บแก้ไข: **`http://localhost:3000/editor.html`** (หรือบน GitHub Pages ของท่าน)
2. คลิกปุ่ม **"ตั้งค่า Google Drive"** ที่แถบเมนูด้านบน
3. วาง URL ที่คัดลอกมาลงในช่อง **"Google Apps Script Web App URL"**
4. คลิกปุ่ม **"ทดสอบการเชื่อมต่อ & บันทึก"**
5. เมื่อขึ้นว่า **"เชื่อมต่อสำเร็จ"** เป็นอันเสร็จสิ้น!

---

## 🌟 ฟังก์ชันการใช้งาน Google Drive ที่เพิ่มเข้ามาในระบบ

1. **อัปโหลดตรงเข้า Google Drive**: 
   - ในหน้า `editor.html` จะมีปุ่ม **"อัปโหลดเข้า Google Drive"** เมื่อเลือกไฟล์จากเครื่อง ระบบจะส่งไฟล์ไปสร้างในโฟลเดอร์ `"KU CSC Hub Images"` ใน Google Drive ของท่านโดยอัตโนมัติ
2. **ระบบแปลงลิงก์ Google Drive อัตโนมัติ (Auto-Converter)**:
   - หากท่านมีรูปภาพใน Google Drive อยู่แล้ว และกดแชร์ลิงก์มา เช่น:
     `https://drive.google.com/file/d/1abc...xyz/view?usp=sharing`
   - ท่านสามารถนำลิงก์นี้มาวางในช่องลิงก์รูปภาพได้ทันที! ระบบจะตรวจจับและแปลงเป็นลิงก์ Direct CDN (`https://lh3.googleusercontent.com/d/1abc...xyz`) ที่แสดงผลบนหน้าเว็บได้ทันที 100%
